# Update these values with the actual Azure resources and ingress addresses for
# the demo cluster before committing. They intentionally live in Git so that the
# rendered manifests stay reproducible.
ingressClass=nginx
keycloakHost=kc.0.0.0.0.nip.io
midpointHost=mp.0.0.0.0.nip.io
cnpgStorageAccount=rwsdemocnpgsa
