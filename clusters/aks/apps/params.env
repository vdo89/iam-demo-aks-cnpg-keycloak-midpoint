# Managed by scripts/render_hosts.py
# Run `python scripts/render_hosts.py --ip <load-balancer-ip>` or `--kubectl`
# after provisioning the ingress controller.
ingressClass=nginx
keycloakHost=kc.0.0.0.0.nip.io
midpointHost=mp.0.0.0.0.nip.io
